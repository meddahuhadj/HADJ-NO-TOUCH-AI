# HADJ NO-TOUCH AI — Python 3.10–3.12 (Windows)
# Core runtime
PySide6>=6.5
opencv-python>=4.8
mediapipe==0.10.21
numpy>=1.24,<2.0
pywin32>=306
pillow>=10.0
psutil>=5.9

# Voice
SpeechRecognition>=3.10
sounddevice>=0.4

# Precise system volume (optional; falls back to media keys)
comtypes>=1.2
pycaw>=20240210

# Offline speech recognition (optional, fully local)
# vosk>=0.3.45

# Development / testing
pytest>=7.0